# Baseline of interactive XAML controls currently missing an accessible name.
# One entry per line: "<relativeFile>:<ControlType>:<x:Name or empty>"
# See XamlAccessibilityScanner.cs for the detection rules.
#
# Currently EMPTY — the ratchet test operates in strict mode. Any new
# interactive control introduced without AutomationProperties.Name /
# LabeledBy / x:Uid / bound-text Content will fail the test. Do not
# append entries here to silence failures; add the missing metadata.
